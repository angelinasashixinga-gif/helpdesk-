import { EstadoLegivelPipe } from './estado-legivel-pipe';

describe('EstadoLegivelPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoLegivelPipe();
    expect(pipe).toBeTruthy();
  });
});
